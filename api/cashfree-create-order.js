const crypto=require('crypto');const {authUser,sb}=require('./_supabase');
module.exports=async function(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{const token=(req.headers.authorization||'').replace(/^Bearer\s+/,'').trim();if(!token)throw new Error('Sign in required');const user=await authUser(token);const plan=req.body?.plan;const plans={seafarer_pro:{amount:499,name:'SailorCareer Seafarer Pro'},employer_pro:{amount:49999,name:'SailorCareer Employer Pro'}};if(plan==='employer_pro'){const company=(await sb(`/rest/v1/companies?user_id=eq.${encodeURIComponent(user.id)}&select=id,verified&limit=1`))[0];if(!company?.id)throw new Error('Employer company profile not found.');const existing=(await sb(`/rest/v1/subscriptions?user_id=eq.${encodeURIComponent(user.id)}&plan=eq.employer_pro&select=id,status,provider_event&order=created_at.desc&limit=1`))[0];if(existing?.provider_event&&/(SUCCESS|PAID)/i.test(String(existing.provider_event))&&existing.status==='pending')throw new Error('Payment already received. Your company is awaiting admin review.');if(company.verified&&existing?.status==='active')throw new Error('Employer subscription is already active.');}if(!plans[plan])return res.status(400).json({error:'Invalid plan'});const p=plans[plan];const env=String(process.env.CASHFREE_ENV||'SANDBOX').trim().toUpperCase();const appId=String(process.env.CASHFREE_APP_ID||'').trim();const secret=String(process.env.CASHFREE_SECRET_KEY||'').trim();const apiVersion=String(process.env.CASHFREE_API_VERSION||'2025-01-01').trim();if(!appId||!secret)throw new Error('Cashfree Production credentials are not configured in Vercel.');const base=env==='PRODUCTION'?'https://api.cashfree.com/pg':'https://sandbox.cashfree.com/pg';const orderId=`SC_${plan}_${user.id}_${Date.now()}`;const cf=await fetch(`${base}/orders`,{method:'POST',headers:{'Content-Type':'application/json','x-api-version':apiVersion,'x-client-id':appId,'x-client-secret':secret,'x-request-id':crypto.randomUUID(),'x-idempotency-key':crypto.randomUUID()},body:JSON.stringify({order_id:orderId,order_amount:p.amount,order_currency:'INR',customer_details:{customer_id:user.id,customer_email:user.email||'',customer_phone:user.phone||'9999999999'},order_meta:{return_url:`${String(process.env.SITE_URL||'https://www.sailorcareer.com').trim()}/employer?payment=return&order_id={order_id}`}})});const data=await cf.json().catch(()=>({}));if(!cf.ok){const msg=String(data?.message||data?.error||'');if(cf.status===401||/authentication|credential|unauthor/i.test(msg))throw new Error(`Cashfree ${env==='PRODUCTION'?'Production':''} authentication failed. Please verify the Production App ID and Secret Key in Vercel.`);throw new Error(msg||`Cashfree order creation failed (${cf.status})`);}if(!data.payment_session_id)throw new Error('Cashfree did not return a payment session.');await sb('/rest/v1/subscriptions',{method:'POST',headers:{Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify({user_id:user.id,plan,status:'pending',amount:p.amount,currency:'INR',provider:'cashfree',provider_order_id:orderId})});res.status(200).json({payment_session_id:data.payment_session_id,order_id:orderId,mode:env==='PRODUCTION'?'production':'sandbox'})}catch(e){res.status(400).json({error:e.message})}};
