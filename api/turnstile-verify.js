module.exports=async function(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{const token=req.body?.token;if(!token) return res.status(400).json({success:false,error:'Missing token'});if(!process.env.TURNSTILE_SECRET_KEY)return res.status(503).json({success:false,error:'Turnstile is not configured'});const r=await fetch('https://challenges.cloudflare.com/turnstile/v0/siteverify',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({secret:process.env.TURNSTILE_SECRET_KEY,response:token,remoteip:req.headers['x-forwarded-for']||''})});const d=await r.json();return res.status(d.success?200:400).json(d)}catch(e){return res.status(500).json({success:false,error:e.message})}};
