module.exports=async function(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});const url=process.env.SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||process.env.SUPABASE_PUBLISHABLE_KEY;const siteKey=process.env.NEXT_PUBLIC_TURNSTILE_SITE_KEY;if(!url||!key||!siteKey)return res.status(503).json({error:'Public authentication configuration is incomplete'});res.setHeader('Cache-Control','no-store');res.status(200).json({supabaseUrl:url,supabaseAnonKey:key,turnstileSiteKey:siteKey});};
